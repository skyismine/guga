# 运行本系统所需的依赖(已在 quant_simple 环境中安装验证)
# 环境: conda 创建 Python 3.11+(推荐 3.11~3.14, 已在 3.14 验证)
#   ⚠️ Python < 3.11 无法安装(vectorbt>=1.1.0 要求 >=3.11,<3.15; akshare 1.18.94 要求 >=3.11;
#      且代码使用 PEP604 类型标注 `int | None`, 需 3.10+)
#   服务器部署:
#     conda create -n quant_simple python=3.11 -y
#     conda activate quant_simple
#     pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/
#     pip install -r requirements.txt
numpy>=2.0
pandas>=2.2
matplotlib>=3.8
requests>=2.31
akshare>=1.16
vectorbt>=1.1.0
scikit-learn>=1.4,<2
lightgbm>=4.6    # 需 >=4.6 兼容新 sklearn(check_array force_all_finite 已移除;旧版+sklearn1.8+ 报错)
flask>=3.0
# Web 图表(plotly python 与内置 plotly.js bundle 必须配套;
#  版本错配会在 import 时报 `Bad property path:scattermapbox`,请用 6.x 并 clean 重装)
plotly>=6.0,<7

# 可选:SilverQuant 实盘/模拟交易(QMT/掘金)依赖,无需实盘可忽略
# xtquant>=250516.1.1
# pywencai>=0.12.3
# mootdx>=0.11.7
